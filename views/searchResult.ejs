<h1>Game</h1>
<% var gameCoverURL = 'https://images.igdb.com/igdb/image/upload/t_cover_big/'%>
<% include partials/searchBar %>

<% gameData.forEach(function(game) { %>
	<% if (game.cover) { %>
		<div class='divider-10'></div>
		
		<div class='form-group'>
			<div class='imgContainer'><img src='<%= gameCoverURL + game.cover.cloudinary_id %>' class='img-responsive coverImgs'></div>
			<a href='/search/searchResult/<%= game.id %>'><%= game.name %></a>
			
			<form method='POST' action='/search/add'>
				<input type='hidden' name='gameId' value='<%= game.id %>'>
				<button type='submit' class='btn btn-primary gradient'>Add to Library</button>
			</form>
		</div>

		<div class='divider-10'></div>
	<% } %>
<% }); %>




